# Figure generation

The current public release verifies the reported behavioral, ERP, FRP, and
eye-tracking statistics from `data/derived/` via `code/analysis/repro_*.py`.

Grand-average ERP/FRP waveform and topomap figure generation requires
time-by-channel evoked data that is not redistributed here.
